<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>All Articles</title>
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 40px;
        background-color: #f9f9f9;
      }

      h1 {
        text-align: center;
        color: #333;
        margin-bottom: 30px;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        background-color: #fff;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
      }

      th, td {
        padding: 12px 16px;
        border-bottom: 1px solid #ddd;
        text-align: left;
      }

      th {
        background-color: #007bff;
        color: white;
      }

      tr:hover {
        background-color: #f1f1f1;
      }

      .btn {
        padding: 6px 12px;
        font-size: 14px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        text-decoration: none;
        color: white;
        margin-right: 6px;
        display: inline-block;
      }

      .btn-edit {
        background-color: #28a745;
      }

      .btn-delete {
        background-color: #dc3545;
      }

      .btn-show {
        background-color: #17a2b8;
      }

      @media (max-width: 600px) {
        table, thead, tbody, th, td, tr {
          display: block;
        }

        tr {
          margin-bottom: 15px;
        }

        td {
          text-align: right;
          padding-left: 50%;
          position: relative;
        }

        td::before {
          content: attr(data-label);
          position: absolute;
          left: 16px;
          width: 45%;
          padding-left: 15px;
          font-weight: bold;
          text-align: left;
        }
      }
    </style>
  </head>

  <body>
    <h1>All Entries in Articles Table</h1>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Description</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @articles.each do |article| %>
          <tr>
            <td data-label="Title"><%= article.tab_title %></td>
            <td data-label="Description"><%= article.tab_description %></td>
            <td data-label="Actions">
              <div style="display: flex; gap: 8px; align-items: center;">
                <%= link_to 'Edit', "/edit/#{article.id}", class: "btn btn-edit" %>
                <%= link_to 'Show', "/showById/#{article.id}", class: "btn btn-show" %>
                <%= button_to "Delete", "/delete/#{article.id}", method: :delete, data: { turbo_confirm: "Are you sure?" }, form: { style: "display:inline;" }, class: "btn btn-delete" %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div style="text-align: center; margin-top: 30px;">
      <%= link_to "Create New Article", "/new", class: "btn btn-edit" %>
    </div>
  </body>
</html>
